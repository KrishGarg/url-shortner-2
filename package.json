{
  "name": "url-shortner-2",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build:frontend": "cd frontend && yarn build",
    "deploy": "yarn build:frontend && deta deploy",
    "start:local": "yarn build:frontend && node index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "deta": "^1.0.1",
    "express": "^4.17.2",
    "nanoid": "^3.2.0",
    "valid-url": "^1.0.9"
  },
  "devDependencies": {
    "dotenv": "^14.3.2"
  }
}
